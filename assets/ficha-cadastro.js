const sessionKey='descendentes_account_session';
const session=JSON.parse(localStorage.getItem(sessionKey)||'null');
if(!session?.accountId){location.replace('cadastro.html');throw new Error('Conta necessária');}
const {db}=await import('../firebase-init.js');
const {ref,get,push,set,update,onValue,remove}=await import('https://www.gstatic.com/firebasejs/12.18.0/firebase-database.js');
const $=id=>document.getElementById(id);
const norm=v=>String(v??'').trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');
const lines=v=>String(v||'').split(/\r?\n/).map(x=>x.trim()).filter(Boolean);
const checked=name=>[...document.querySelectorAll(`input[name="${name}"]:checked`)].map(x=>x.value);
const activities=['Clube de Esgrima','Clube de Magia','Clube de Alquimia','Clube de Criaturas Mágicas','Clube de Teatro','Clube de Música','Clube de Dança','Clube de Artes','Clube de Investigação','Clube de Estratégia','Clube de História','Clube de Exploração','Clube de Arqueologia Mágica','Clube de Poções e Remédios','Clube de Herbologia','Clube de Arco e Flecha','Clube de Combate','Clube de Voo','Clube de Moda e Design','Clube de Culinária','Clube de Jardinagem','Clube de Literatura','Clube de Escrita','Jornal da Auradon Prep','Clube de Debate','Clube de Diplomacia','Clube de Voluntariado','Clube de Tecnologia','Clube de Invenções','Clube de Jogos','Clube de Esportes','Grêmio Estudantil','Clube da Coroa'];
let affiliations={},students={},account={},accountId=session.accountId,draft={};
async function hashPassword(v){const d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v));return [...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,'0')).join('')}
function showStep(n){document.querySelectorAll('.ficha-step').forEach(x=>x.classList.toggle('active',x.dataset.panel===String(n)));document.querySelectorAll('.progress-step').forEach(x=>x.classList.toggle('active',x.dataset.step===String(n)));saveDraft({step:n});window.scrollTo({top:0,behavior:'smooth'})}
function fillActivities(){for(const id of ['activityPrimary','activitySecondary']){const s=$(id);for(const a of activities){const o=document.createElement('option');o.value=a;o.textContent=a;s.appendChild(o)}}}
function countLines(id,out,min,label){const n=lines($(id).value).length;$(out).textContent=`${n}/${min} ${label}`;return n>=min}
function countPowers(){const n=lines($('powers').value).length;$('powersCount').textContent=`${n}/4 poderes`;return n<=4}
function currentCount(key,a){return Object.values(students||{}).filter(Boolean).filter(s=>s.affiliationKey===key||(norm(s.filiation||s.progenitors)===norm(a?.name)&&norm(s.affiliationStudio||s.studio)===norm(a?.studio)&&norm(s.affiliationStory||s.story)===norm(a?.story))).length}
function renderAffiliations(){const studio=$('studio'),story=$('fairyTale'),progenitor=$('progenitor');studio.innerHTML='<option value="">Selecione o estúdio...</option>';story.innerHTML='<option value="">Selecione primeiro o estúdio...</option>';progenitor.innerHTML='<option value="">Selecione primeiro o conto de fadas...</option>';story.disabled=true;progenitor.disabled=true;const studios={};Object.entries(affiliations||{}).forEach(([key,a])=>{if(!a)return;const st=a.studio||a.franquia||'Outros',sn=a.story||a.conto||a.fairyTale||'Outros';(studios[st]??={})[sn]??=[];studios[st][sn].push({key,a})});Object.keys(studios).sort((a,b)=>a.localeCompare(b,'pt-BR')).forEach(st=>{const o=document.createElement('option');o.value=st;o.textContent=st;studio.appendChild(o)});}
function renderStories(){const st=$('studio').value,story=$('fairyTale'),progenitor=$('progenitor');story.innerHTML='<option value="">Selecione o conto de fadas...</option>';progenitor.innerHTML='<option value="">Selecione primeiro o conto de fadas...</option>';progenitor.disabled=true;story.disabled=!st;if(!st)return;const stories={};Object.entries(affiliations||{}).forEach(([key,a])=>{if(!a)return;const as=a.studio||a.franquia||'Outros',sn=a.story||a.conto||a.fairyTale||'Outros';if(as===st)(stories[sn]??=[]).push({key,a})});Object.keys(stories).sort((a,b)=>a.localeCompare(b,'pt-BR')).forEach(name=>{const o=document.createElement('option');o.value=name;o.textContent=name;story.appendChild(o)})}
function selectAffiliation(){const st=$('studio').value,sn=$('fairyTale').value,s=$('progenitor'),status=$('progenitorStatus');s.innerHTML='<option value="">Selecione o progenitor...</option>';s.disabled=true;status.className='filiation-status wait';status.textContent='Aguardando seleção.';if(!st||!sn)return;const items=Object.entries(affiliations||{}).filter(([key,a])=>a&&(a.studio||a.franquia||'Outros')===st&&(a.story||a.conto||a.fairyTale||'Outros')===sn);if(!items.length){status.textContent='Nenhum progenitor encontrado no banco.';return}items.sort((x,y)=>String(x[1].name||'').localeCompare(String(y[1].name||''),'pt-BR'));for(const [key,a] of items){const max=Number(a.maxChildren??a.maxFilhos??3)||3,current=currentCount(key,a),o=document.createElement('option');o.value=key;o.textContent=`${a.name||'Sem nome'} (${current}/${max})`;o.disabled=current>=max;s.appendChild(o)}s.disabled=false}
function updateProgenitorStatus(){const key=$('progenitor').value,status=$('progenitorStatus');if(!key||!affiliations[key]){status.className='filiation-status wait';status.textContent='Selecione um progenitor.';return}const a=affiliations[key],max=Number(a.maxChildren??a.maxFilhos??3)||3,current=currentCount(key,a);status.className=current>=max?'filiation-status full':'filiation-status ready';status.textContent=current>=max?`🔴 Progenitor indisponível — ${current}/${max}`:`✓ ${current}/${max} vagas utilizadas — ${Math.max(0,max-current)} vaga(s) disponível(is)`}
function restoreFiliation(){const st=draft.affiliationStudio||'',sn=draft.affiliationStory||draft.fairyTale||'',key=draft.affiliationKey||'';if(st){$('studio').value=st;renderStories();if(sn){$('fairyTale').value=sn;selectAffiliation();if(key){$('progenitor').value=key;updateProgenitorStatus()}}}}
function collectDraft(){return {accountId,step:document.querySelector('.ficha-step.active')?.dataset.panel||2,playerName:$('playerName').value.trim(),playerAge:$('playerAge').value||'',availability:checked('availability'),characterName:$('characterName').value.trim(),characterAge:$('characterAge').value||'',sexuality:$('sexuality').value.trim(),affiliationKey:$('progenitor').value||'',affiliationStudio:$('studio').value||'',affiliationStory:$('fairyTale').value||'',fairyTale:$('fairyTale').value||'',progenitors:affiliations[$('progenitor').value]?.name||'',royalRebel:$('royalRebel').value,extracurricularPrimary:$('activityPrimary').value,extracurricularSecondary:$('activitySecondary').value,powers:$('powers').value,history:$('history').value,personality:$('personality').value,avatar:$('avatar').value,avatarImage:$('avatarFile').dataset.data||draft.avatarImage||'',updatedAt:Date.now(),status:'rascunho'}};
let saveTimer;
async function saveDraft(extra={}){clearTimeout(saveTimer);saveTimer=setTimeout(async()=>{try{const data={...collectDraft(),...extra};await update(ref(db,'site/fichasRascunhos/'+accountId),data);draft=data}catch(e){console.error('Falha ao salvar rascunho',e)}},350)}
function applyDraft(d){draft=d||{};const map={playerName:'playerName',playerAge:'playerAge',characterName:'characterName',characterAge:'characterAge',sexuality:'sexuality',royalRebel:'royalRebel',activityPrimary:'extracurricularPrimary',activitySecondary:'extracurricularSecondary',powers:'powers',history:'history',personality:'personality',avatar:'avatar'};Object.entries(map).forEach(([id,k])=>{if(d[k]!=null&&$(id))$(id).value=d[k]});document.querySelectorAll('input[name="availability"]').forEach(x=>x.checked=(d.availability||[]).includes(x.value));if(d.avatarImage)$('avatarFile').dataset.data=d.avatarImage;countLines('history','historyCount',20,'linhas');countLines('personality','personalityCount',20,'linhas');countPowers();restoreFiliation();const step=Number(d.step)===3?3:2;showStep(step)}
fillActivities();
['history','personality'].forEach(id=>$(id).addEventListener('input',()=>{countLines(id,id+'Count',20,'linhas');saveDraft()}));$('powers').addEventListener('input',()=>{countPowers();saveDraft()});
['playerName','playerAge','characterName','characterAge','sexuality','royalRebel','activityPrimary','activitySecondary','avatar'].forEach(id=>$(id)?.addEventListener('input',()=>saveDraft()));
document.querySelectorAll('input[name="availability"]').forEach(x=>x.addEventListener('change',()=>saveDraft()));
$('studio').onchange=()=>{renderStories();saveDraft()};$('fairyTale').onchange=()=>{selectAffiliation();saveDraft()};$('progenitor').onchange=()=>{updateProgenitorStatus();saveDraft()};$('activityPrimary').onchange=()=>{[...$('activitySecondary').options].forEach(o=>o.hidden=o.value===$('activityPrimary').value);saveDraft()};
$('avatarFile').onchange=()=>{const f=$('avatarFile').files[0];if(!f){$('avatarPreview').innerHTML='';return}if(f.size>6*1024*1024){$('characterError').textContent='A foto deve ter no máximo 6 MB.';$('avatarFile').value='';return}const r=new FileReader();r.onload=()=>{$('avatarFile').dataset.data=r.result;$('avatarPreview').innerHTML=`<img src="${r.result}" alt="Prévia do avatar">`;saveDraft()};r.readAsDataURL(f)};
$('backTo2').onclick=()=>showStep(2);
$('logoutDraft').onclick=()=>{localStorage.removeItem(sessionKey);location.href='index.html'};
onValue(ref(db,'site/filiacoes'),snap=>{affiliations=snap.val()||{};renderAffiliations();restoreFiliation()},err=>{$('fairyTale').innerHTML='<option value="">Não foi possível carregar as filiações</option>';console.error(err)});
onValue(ref(db,'site/alunos'),snap=>{students=snap.val()||{};updateProgenitorStatus()},err=>console.error(err));
const [acctSnap,draftSnap]=await Promise.all([get(ref(db,'site/contas/'+accountId)),get(ref(db,'site/fichasRascunhos/'+accountId))]);
if(!acctSnap.exists()){localStorage.removeItem(sessionKey);location.replace('cadastro.html');throw new Error('Conta não encontrada')}
account=acctSnap.val();if(account.fichaId){location.replace('ficha-status.html');throw new Error('Ficha já enviada')}
if(draftSnap.exists())applyDraft(draftSnap.val());else{$('playerName').value=account.name||account.playerName||'';showStep(2)}
$('aboutForm').onsubmit=async e=>{e.preventDefault();const err=$('aboutError');err.textContent='';const age=Number($('playerAge').value),av=checked('availability');if(!age)return err.textContent='Informe sua idade.';if(age<16)return err.textContent='Idade não permitida. Este RPG é recomendado para maiores de 16 anos.';if(!av.length)return err.textContent='Selecione pelo menos uma disponibilidade para cena.';if(!$('playerName').value.trim())return err.textContent='Informe seu nome.';try{await update(ref(db,'site/contas/'+accountId),{name:$('playerName').value.trim(),playerName:$('playerName').value.trim(),playerAge:age,availability:av,status:'criando_ficha',updatedAt:Date.now()});await saveDraft({step:3});showStep(3)}catch(x){err.textContent='Não foi possível salvar seus dados no banco de dados.'}};
$('characterForm').onsubmit=async e=>{e.preventDefault();const err=$('characterError');err.textContent='';const powers=lines($('powers').value),history=lines($('history').value),personality=lines($('personality').value),primary=$('activityPrimary').value,secondary=$('activitySecondary').value,key=$('progenitor').value,a=affiliations[key];if(!$('characterName').value.trim())return err.textContent='Informe o nome do personagem.';if(!Number($('characterAge').value))return err.textContent='Informe a idade do personagem.';if(!key||!a)return err.textContent='Selecione um progenitor disponível.';const max=Number(a.maxChildren??a.maxFilhos??3)||3;if(currentCount(key,a)>=max)return err.textContent='Esta filiação acabou de ficar sem vagas. Escolha outra.';if(!primary)return err.textContent='Selecione a atividade extracurricular principal.';if(secondary===primary)return err.textContent='A atividade secundária deve ser diferente da principal.';if(powers.length<1||powers.length>4)return err.textContent='Informe entre 1 e 4 poderes.';if(history.length<20)return err.textContent='A história precisa ter no mínimo 20 linhas.';if(personality.length<20)return err.textContent='A personalidade precisa ter no mínimo 20 linhas.';const avatarName=$('avatar').value.trim(),avatarImage=$('avatarFile').dataset.data||'';if(!avatarName&&!avatarImage)return err.textContent='Escreva o nome do avatar ou faça upload de uma foto.';const btn=e.target.querySelector('button[type=submit]');btn.disabled=true;try{const id=push(ref(db,'site/fichas')).key,token=crypto.randomUUID(),data={accountId,playerName:$('playerName').value.trim(),login:account.login,loginLower:norm(account.login),email:account.email||'',passwordHash:account.passwordHash,playerAge:Number($('playerAge').value),availability:checked('availability'),characterName:$('characterName').value.trim(),characterAge:Number($('characterAge').value),sexuality:$('sexuality').value.trim(),affiliationKey:key,affiliationStudio:a.studio||a.franquia||'',affiliationStory:a.story||a.conto||a.fairyTale||'',fairyTale:a.story||a.conto||a.fairyTale||'',progenitors:a.name||'',filiation:a.name||'',royalRebel:$('royalRebel').value,extracurriculars:[primary,...(secondary?[secondary]:[])],extracurricularPrimary:primary,extracurricularSecondary:secondary||'',powers:powers.join('\n'),history:history.join('\n'),personality:personality.join('\n'),avatar:avatarName,avatarUrl:avatarImage,status:'em_analise',accessToken:token,createdAt:Date.now(),submittedAt:Date.now(),updatedAt:Date.now()};await set(ref(db,'site/fichas/'+id),data);await update(ref(db,'site/contas/'+accountId),{status:'ficha_em_analise',fichaId:id,updatedAt:Date.now()});await remove(ref(db,'site/fichasRascunhos/'+accountId));localStorage.setItem('descendentes_ficha_access',JSON.stringify({id,token,accountId}));location.href='ficha-concluida.html'}catch(x){err.textContent=x.message||'Não foi possível enviar sua ficha.'}finally{btn.disabled=false}};
